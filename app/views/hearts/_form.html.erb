<%= form_with(model: heart, local: true) do |form| %>
  <% if heart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(heart.errors.count, "error") %> prohibited this heart from being saved:</h2>

      <ul>
      <% heart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <%= form_for @heart do |f| %>
    <%= form.label :feeling %>
    <%= form.select :mind_id, Mind.all.map{ |b| [b.name, b.id] } %>
  </div>

  <div class="field">
    <%= form.label :日付 %>
    <%= form.date_select :date, id: :heart_date %>  
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  <% end %>

<% end %>
