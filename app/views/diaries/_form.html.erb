<%= form_with(model: diary, local: true) do |form| %>
  <% if diary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diary.errors.count, "error") %> prohibited this diary from being saved:</h2>

      <ul>
      <% diary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "日付" %>
    <%= form.date_select :date, id: :diary_date %>
  </div>

  <div class="field">
    <%= form.label "内容" %>
    <%= form.text_area :comment, id: :diary_comment %>
  </div>

  <div class="field">
    <%= form.label "写真" %>
    <%= form.text_field :photo, id: :diary_photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
