<%= form_with(model: mind, local: true) do |form| %>
  <% if mind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mind.errors.count, "error") %> prohibited this mind from being saved:</h2>

      <ul>
      <% mind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
